export const ADDON_ID = "package-version";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `package-version`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
