export const ADDON_ID = "storybook-addon-package-version";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `storybook-addon-package-version`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
